# Cloudflare Pages — Redirects
# (same format as Netlify _redirects)

# Root → English version
/                   /en/                302

# Clean URLs
/en                 /en/index.html      200
/en/app             /en/app.html        200
/app                /app.html           200
/login.html         /en/login.html      302

# SPA fallback — must be last
/*                  /index.html         200
