# Cloudflare Pages — Redirects

# Root → English landing page (handled by saas/index.html static file)

# GR edition — clean URLs (funeralos.net/gr/...)
/gr/login           /login.html         200
/gr/app             /app.html           200
/gr/reset           /reset.html         200
/gr/success         /success.html       200

# EN edition — Cloudflare Pages Pretty URLs handles /en/login, /en/app, etc. natively
# (no rules needed here — adding 200 rewrites conflicts with Pretty URL normalization)
